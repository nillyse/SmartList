import { ToogleClassDirective } from './toogle-class.directive';

describe('ToogleClassDirective', () => {
  it('should create an instance', () => {
    const directive = new ToogleClassDirective();
    expect(directive).toBeTruthy();
  });
});
